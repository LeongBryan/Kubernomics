import type { SchedulerStrategy } from '../types'

export const DEFAULT_SCHEDULER_STRATEGY: SchedulerStrategy = 'Spread'

export const SCHEDULER_STRATEGIES: SchedulerStrategy[] = ['Spread', 'Bin-pack']
